/**
 * A navigable target used internally by spotlight's container and target resolution
 * algorithms: either an actual DOM element or a string referring to a container id.
 */
export type ContainerTarget = Element | string;
