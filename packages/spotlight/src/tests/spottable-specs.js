import React from 'react';

describe('Spottable Specs', () => {

	it('should evaluate 2 variables as a test', function () {
		
		const expected = 1;
		const actual = 1;

		expect(actual).to.equal(expected);
	});
});
